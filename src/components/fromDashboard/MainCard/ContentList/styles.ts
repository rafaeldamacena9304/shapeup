import styled from "styled-components";

export const ContentList = styled.ul`
  list-style: none;
  color: var(--gray);
  font-size: 14px;

  li {
    margin-bottom: 8px;
    font-weight:600;
  }
  li > span{
    margin-left:12px;
    font-weight:400;
  }
`;
